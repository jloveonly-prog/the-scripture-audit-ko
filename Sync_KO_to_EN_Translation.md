# 한글 -> 영문 복사 및 번역 동기화 작업 지시서

이 문서는 `D:\01.TheScriptureAudit_ko` 프로젝트의 한글 원본 파일들을 `D:\01.TheScriptureAudit` 영문 프로젝트로 번역 및 동기화하기 위해 AI 에이전트에게 지시하는 워크플로우 문서입니다.

사용자가 이 문서로 작업을 지시하거나 정기적 실행을 요청하면 아래 절차에 따라 작업을 수행하십시오.

## 🗂️ 0. 대상 프로젝트 매핑

아래 두 프로젝트가 동기화 대상입니다. 매번 실행 시 **둘 다** 확인하십시오. (한쪽만 확인하고 끝내지 말 것)

| 프로젝트 | 소스 경로 (KO) | 타겟 경로 (EN) | 비고 |
|---|---|---|---|
| 성경 감사 (Scripture Audit) | `D:\01.TheScriptureAudit_ko\the-scripture-audit` | `D:\01.TheScriptureAudit\the-scripture-audit` | 하위 폴더는 `05_REPORT(전과보고서)` -> `05_REPORT`처럼 괄호 안 한글 설명만 제거하고 번호/영문 약어는 그대로 유지 |
| 설교 감사 (Sermon Audit) | `D:\01.TheScriptureAudit_ko\the-sermon-audit` | `D:\01.TheScriptureAudit\the-sermon-audit` | ⚠️ EN 쪽 폴더 구조가 오래되어 KO와 어긋나 있을 수 있음(예: KO는 `02_TEMPLATE(템플릿·예시)` + `03_REPORT(설교감사보고서)`로 분리되어 있으나 EN은 옛 구조인 `02_REPORT` 하나만 있는 상태였음). **Step 1에서 폴더 구조 자체의 diff를 먼저 확인**하고, 필요하면 EN 쪽 폴더를 새로 만들거나 이름을 맞춰야 함 |

### 제외 대상 (번역/동기화하지 않음)
- `D:\01.TheScriptureAudit_ko\the-scripture-audit\00_THESCRIPTURE(성경원문)` — KJV 원문/대역 JSON·TXT 데이터 묶음(수 MB~수십 MB, 저작권 보호 대상, KO `.gitignore`에도 명시됨). 이미 원문+대역 데이터라 "번역"의 대상이 아니며, EN 저장소로 복사하지 않습니다.
- `D:\01.TheScriptureAudit_ko\the-catholic-audit` — 아직 EN 프로젝트에 대응 폴더 자체가 없는 별도 프로젝트입니다. 사용자가 명시적으로 요청하기 전까지는 동기화 대상에 포함하지 마십시오.
- KO 저장소 `.gitignore`에 등록된 경로(`.obsidian`, `docs/`, `99.참고자료/`, `_Legacy_Engines/`, `_INBOX(...)/` 내부 실작업 파일, `the-catholic-audit/05_DOCTRINE_DB` 등) — 애초에 `git status`/`git diff`에 잡히지 않지만, 혹시 수동으로 훑어볼 때도 대상에서 제외합니다.

## 📋 작업 절차 (Workflow)

### 1. Git 히스토리 확인 및 대상 변경사항 선정
- 위 매핑표의 **각 프로젝트 경로별로** 소스 경로(`_ko`)에서 `git status`와 `git diff -- <경로>`를 실행하여 **새로 생성된 파일**, **수정된(Modified) 파일**, **삭제된(Deleted) 파일**, **이름이 바뀐(Renamed) 파일**을 모두 식별합니다.
  - 삭제된 파일: EN 쪽 대응 파일도 삭제할지, 아니면 이력 보존 차원에서 남겨둘지 사용자에게 확인 후 처리합니다(자동으로 삭제하지 않음).
  - 이름이 바뀐 파일: 새 파일 취급으로 번역하지 말고, EN 쪽 대응 파일명도 함께 변경(rename)해서 히스토리를 유지합니다.
- 파일 단위 변경 확인과 별도로, **폴더 구조 자체가 바뀌었는지**(새 폴더 생성, 폴더 분리/합병, 번호 재부여 등)도 함께 확인합니다. 폴더 구조가 바뀐 경우 파일을 개별 번역하기 전에 EN 쪽 폴더 구조부터 맞춥니다.
- 단순 오타 수정인지, 새로운 내용/문서 링크가 추가된 것인지 파악하여 동기화할 범위(전체 재번역 vs 부분 수정 반영)를 결정합니다.

### 2. 파일명 및 내용 전체 영문 번역 (서브 에이전트 활용)
- 선정된 한글 원본 파일의 **내용(Content)**을 영어로 번역합니다. 신학적 의미가 훼손되지 않도록 주의합니다.
- **번역을 시작하기 전에 `Report_Index.md`를 먼저 훑어서, 같은 인물·용어·개념에 대해 이미 사용된 기존 영문 표현이 있는지 확인**하고 동일하게 재사용하십시오. (같은 개념이 파일마다 다르게 번역되면 검색성과 일관성이 깨집니다.)
- **파일명(File Name)**을 적절한 영어로 번역하여 영문 소스의 타겟 경로에 생성합니다. (예: `REPORT_첫번째_세상민족.md` -> `REPORT_First_World_Nations.md`)
- 파일 크기가 크거나 여러 파일인 경우, AI는 스스로 **Subagent(서브 에이전트)**를 다중으로 생성하여 병렬 번역을 지시하십시오.

### 3. 내부 링크 및 '연관 문서' 동기화 (파이썬 스크립트 권장)
- **가장 자주 누락되는 부분입니다:** 특정 한글 파일 내부의 링크나 하단 '연관 문서 인덱스'가 추가/수정되었다면, **해당 디렉토리 내의 다른 모든 영문 파일들에도 이 링크 업데이트가 반영되어야 하는지(Cascading Update) 반드시 확인**하십시오.
- `Report_Index.md`를 참고하여 한글 파일명과 매핑되는 영문 파일명을 확인합니다.
- 다수의 영문 파일에 동일한 링크 수정/추가가 필요할 경우, 수동으로 하지 말고 **Python 스크립트를 작성하여 일괄 치환(Bulk Replace)**하는 것을 강력히 권장합니다.
- 일괄 치환 후에는 **깨진 링크(대상 파일이 존재하지 않는 상대경로/앵커)가 없는지 스크립트로 검증**하고, 발견되면 결과 보고에 반드시 포함합니다.

### 4. `Report_Index.md` 리스트업 및 경로 동기화
- **인덱스 파일 경로:** `D:\01.TheScriptureAudit\Report_Index.md`
- 번역 및 생성이 끝난 파일들은 프로젝트에 맞는 기존 `📁 Folder:` 섹션을 찾아 매핑 리스트에 추가합니다. 해당 섹션이 없으면(sermon-audit 등 신규 카테고리) 새 섹션을 만듭니다.
- 형식: `| [한글 파일명](한글 깃헙 URL) | [영문 파일명](영문 깃헙 URL) |`

### 5. 커밋 처리
- 변경/생성/삭제한 파일은 **EN 저장소(`D:\01.TheScriptureAudit`)에서만** 커밋합니다. KO 원본 저장소(`_ko`)는 읽기 전용으로 취급하고 수정하지 않습니다.
- 커밋은 논리적 단위(파일 그룹, 또는 프로젝트별)로 나누어 명확한 메시지로 작성합니다.
- **`git push`는 사용자가 명시적으로 요청한 경우에만** 수행합니다. 자동 실행 시에도 push까지는 하지 않고 커밋까지만 진행한 뒤 보고합니다.

### 6. 결과 보고
- 처리한 파일 목록(원본 파일명 -> 영문 파일명, 프로젝트별로 구분)을 보고합니다.
- 링크 일괄 업데이트 결과와 깨진 링크 검증 결과를 보고합니다.
- 폴더 구조를 새로 만들거나 바꾼 경우 그 내역을 보고합니다.
- 삭제/rename 처리가 필요했지만 사용자 확인이 필요해 보류한 항목이 있으면 목록으로 보고합니다.
- 제외 대상(00_THESCRIPTURE, the-catholic-audit 등)에 새 변경사항이 있었다면, 처리하지 않았음을 알리되 내용은 요약해서 알려줍니다.

---

> [!NOTE]
> AI 에이전트(Antigravity 등)는 사용자가 **"Sync_KO_to_EN_Translation.md 실행해"** 라고만 지시하더라도, 위 0~6단계를 모두 스스로 기획하고, 스크립트와 서브 에이전트를 적극 활용하여 자동화된 방식으로 끝마쳐야 합니다. 단, **삭제/rename 여부 확정**과 **git push**는 사용자 확인 없이 임의로 처리하지 않습니다.
